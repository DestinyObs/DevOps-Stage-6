---
# Ansible Playbook for DevOps Stage 6 Deployment

- name: Deploy DevOps Stage 6 TODO Application
  hosts: app_server
  gather_facts: yes

   # Fix for Ansible privilege escalation with unprivileged user
  vars:
    ansible_common_remote_group: "{{ deploy_user }}"
  
  roles:
    - role: dependencies
      tags: ['dependencies', 'setup']
    
    - role: deploy
      tags: ['deploy', 'app']
